# Dữ liệu và model — KHÔNG commit (dung lượng lớn + quyền riêng tư, xem CLAUDE.md mục 10).
# Neo vào gốc repo: tests/data/ (dữ liệu mẫu nhỏ cho test) vẫn commit được.
/data/
/models/
*.engine
*.onnx
*.etlt
*.pt
*.mp4
*.avi
*.mkv

# Fixture tổng hợp: sinh lại được bằng `make fixture`, deterministic theo seed nên ai
# cũng ra file giống hệt. ~845K mỗi lần sinh, commit vào chỉ tổ phình repo.
# Fixture ghi từ pipeline thật (M3) KHÔNG sinh lại được — xem tests/fixtures/README.md.
tests/fixtures/*.jsonl
tests/fixtures/*.gt.json

# Kết quả chạy
eval/results/
outputs/
*.db
*.sqlite3

# Môi trường
.env
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
build/
dist/

# Hệ điều hành / IDE
.DS_Store
.idea/
.vscode/
